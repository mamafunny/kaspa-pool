apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kaspad-persist-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: standard-rwo
